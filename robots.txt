# Costo SV — robots.txt  (BORRADOR — BLOQUEADO hasta definir el dominio)
# La línea Sitemap usa el marcador %%SITE_URL%%. La validación pre-deploy
# (predeploy-check.mjs) FALLA mientras el marcador siga sin resolver.
# No publicar con un dominio inventado.

User-agent: *
Allow: /

# admin.html NO se bloquea aquí a propósito: un `Disallow` impediría que el crawler
# LEA la etiqueta <meta robots="noindex"> y podría dejar la URL indexada "a ciegas".
# La protección real es el `noindex` en admin.html + no enlazarla + excluirla del sitemap.
# La solución definitiva será autenticación o directamente no desplegar ese archivo.

Sitemap: %%SITE_URL%%/sitemap.xml
